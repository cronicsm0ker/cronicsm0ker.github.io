# @roofops/mobile

Expo React Native app for field/sales/owner. See `docs/plans/phase-0.md` Step 6:

- React Navigation, `LoginScreen` + `HomeScreen`
- Shared `@roofops/api-client` with `expo-secure-store` token adapter
- Phase 0 ships running via Expo Go against staging API (EAS internal distribution slips to early Phase 1)

Implementation begins after API + shared client stabilize.
