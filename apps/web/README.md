# @roofops/web

React + Vite admin/back-office app. See `docs/plans/phase-0.md` Step 5 for the Phase 0 scope:

- Routes: `/login`, `/` (protected), `/forgot`, `/reset/:token`
- TanStack Router + TanStack Query + Zustand auth store
- Tailwind + shadcn/ui
- Cloudflare Pages deploy

Implementation begins after the API foundation lands.
